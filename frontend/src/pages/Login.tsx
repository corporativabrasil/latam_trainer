import {ArrowRight,BookOpen,Languages,Mic2,Sparkles} from 'lucide-react';import {FormEvent,useState} from 'react';import {useNavigate} from 'react-router-dom';import {api} from '../services/api';
export default function Login(){const n=useNavigate();const [email,setEmail]=useState('admin@corporativabrasil.com.br');const [password,setPassword]=useState('Admin@123');const [err,setErr]=useState('');async function submit(e:FormEvent){e.preventDefault();setErr('');try{const r=await api.post('/auth/login',{email,password});localStorage.setItem('token',r.data.access_token);n('/')}catch{setErr('E-mail ou senha inválidos.')}}return <div className="login-page"><section className="login-showcase"><div className="login-brand"><div className="brand-mark">CB</div><div><strong>Corporativa</strong><span>LATAM Trainer AI</span></div></div><div className="showcase-copy"><span className="kicker light">SEU COPILOTO PARA TREINAMENTOS INTERNACIONAIS</span><h1>Prepare seu conteúdo.<br/>Treine seu espanhol.<br/>Entre em sala com segurança.</h1><p>Transforme seus materiais em português em experiências de aprendizagem adaptadas para a América Latina.</p><div className="feature-pills"><span><Languages/>Tradução contextual</span><span><Mic2/>Pronúncia guiada</span><span><Sparkles/>Simulações com IA</span><span><BookOpen/>Manual do instrutor</span></div></div><small>Corporativa Brasil · Educação Corporativa desde 2007</small></section><section className="login-form-area"><form className="login-card" onSubmit={submit}><span className="kicker">ACESSO DO INSTRUTOR</span><h2>Bem-vindo de volta</h2><p>Entre para continuar sua preparação.</p><div className="field"><label>E-mail</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>Senha</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></div>{err&&<div className="error-box">{err}</div>}<button className="btn primary full">Entrar na plataforma <ArrowRight size={18}/></button><div className="demo-note">Ambiente inicial configurado para a Corporativa Brasil.</div></form></section></div>}
