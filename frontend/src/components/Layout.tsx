import {BarChart3,BookOpen,Languages,LogOut,Menu,MessageCircleMore,Mic2,PanelLeftClose,PanelLeftOpen,Sparkles} from 'lucide-react';
import {useState} from 'react';import {NavLink,Outlet,useNavigate} from 'react-router-dom';
const links=[['/','Visão geral',BarChart3],['/projects','Treinamentos',BookOpen],['/studio','Estúdio bilíngue',Languages],['/learn','Plano de preparação',Sparkles],['/practice','Simulador IA',MessageCircleMore],['/pronunciation','Pronúncia',Mic2]] as const;
export default function Layout(){const n=useNavigate();const [collapsed,setCollapsed]=useState(false);return <div className={`app-shell ${collapsed?'collapsed':''}`}><aside className="sidebar"><div className="brand-wrap"><div className="brand-mark">CB</div><div className="brand-copy"><strong>Corporativa</strong><span>LATAM Trainer AI</span></div></div><button className="collapse-btn" onClick={()=>setCollapsed(!collapsed)}>{collapsed?<PanelLeftOpen size={19}/>:<PanelLeftClose size={19}/>}</button><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/' } title={label}><Icon size={20}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-bottom"><div className="ai-badge"><Sparkles size={18}/><div><b>Copiloto ativo</b><small>Preparação em espanhol</small></div></div><button className="logout" onClick={()=>{localStorage.removeItem('token');n('/login')}}><LogOut size={19}/><span>Sair</span></button></div></aside><main className="workspace"><header className="topbar"><div><button className="mobile-menu"><Menu size={20}/></button><span className="eyebrow">AMBIENTE DE PREPARAÇÃO DO INSTRUTOR</span></div><div className="top-user"><div className="status-dot"/><div><b>Prof. Márcio Silva</b><small>Instrutor</small></div><div className="avatar">MS</div></div></header><section className="content"><Outlet/></section></main></div>}
